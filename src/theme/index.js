import Theme from "./ThemeProvider";

export {
  ColorModeContext,
  defaultColorMode,
  darkMode,
  lightMode,
} from "./mode";

export const vars = {
  headerHeight: 64,
  headerHeightSm: 56,
  drawerWidth: 240,
};

export default Theme;
